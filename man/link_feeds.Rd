% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/staging.R
\name{link_feeds}
\alias{link_feeds}
\title{Create a FEEDS Relationship}
\usage{
link_feeds(building, links, proceed = NULL, verbose = TRUE)
}
\arguments{
\item{building}{Character or integer corresponding to a single building name or id. Only one value is allowed (functions using this parameter operate on a single building).}

\item{links}{Data.frame, one row per link: `source_id` (published
equipment id of the feeding equipment) plus `location_name` and/or
`equipment_id` (the fed target for that row - leave the other `NA`)}

\item{proceed}{(Optional) Logical argument indicating whether to proceed operation without asking for explicit user input. Useful for scripting}

\item{verbose}{Logical; if `TRUE`, prints progress messages (and for `api.request()` specifically, full HTTP request/response details). Defaults to `TRUE`.}
}
\value{
(Conditional) A named list, one entry per source equipment id,
  with the result of each link/feeds request.
}
\description{
Equipment can feed locations and/or other equipment.
}
\examples{
\dontrun{
links <- data.frame(
  source_id = c(90094, 90094, 90094),
  location_name = c("Room-1", "ROOM-2", NA),
  equipment_id = c(NA, NA, 23321)
)
link_feeds(building = 876, links = links, proceed = TRUE)
}

}
