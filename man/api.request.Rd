% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{api.request}
\alias{api.request}
\title{Make an API Request with httr2}
\usage{
api.request(
  endpoint,
  method = "GET",
  request_body = NULL,
  file = NULL,
  response_body = "string",
  verbose = TRUE
)
}
\arguments{
\item{endpoint}{A character string specifying the API endpoint (appended to the base URL).}

\item{method}{A character string specifying the HTTP method to use. One of `"GET"`, `"POST"`, `"PATCH"`, or `"DELETE"`. Defaults to `"GET"`.}

\item{request_body}{An optional list or named object to send as JSON in the request body.}

\item{file}{An optional character string specifying the path to a file for upload. Used only with `"POST"` requests.}

\item{response_body}{A character string indicating how to parse the response. One of `"string"` or `"json"`. Defaults to `"string"`}

\item{verbose}{Logical; if `TRUE`, prints progress messages (and for `api.request()` specifically, full HTTP request/response details). Defaults to `TRUE`.}
}
\value{
A parsed API response. If `response_body = "string"`, the response is converted to a list or dataframe. If `"json"`, returns the raw JSON structure as a list.
}
\description{
A general-purpose function to make authenticated API requests to Onboard's API. Supports multiple HTTP methods, JSON request bodies, and file uploads.
}
\details{
This function wraps `httr2` functionality for making authenticated requests to Onboard’s API.
It uses credentials retrieved from `api.access()` function that must return a named list with elements: `url`, `key`, and `token`.

If both `file` and `request_body` are provided, the function will raise an error to avoid ambiguity in the request body.
}
