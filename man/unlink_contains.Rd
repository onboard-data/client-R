% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/staging.R
\name{unlink_contains}
\alias{unlink_contains}
\title{Remove a CONTAINS Relationship}
\usage{
unlink_contains(building, links, proceed = NULL, verbose = TRUE)
}
\arguments{
\item{building}{Character or integer corresponding to a single building name or id. Only one value is allowed (functions using this parameter operate on a single building).}

\item{links}{Data.frame, one row per link: `source_location_names` (name
of the containing location) plus `location_name` and/or `equipment_id`
(the contained target for that row - leave the other `NA`)}

\item{proceed}{(Optional) Logical argument indicating whether to proceed operation without asking for explicit user input. Useful for scripting}

\item{verbose}{Logical; if `TRUE`, prints progress messages (and for `api.request()` specifically, full HTTP request/response details). Defaults to `TRUE`.}
}
\value{
(Conditional) A named list, one entry per source location name,
  with the result of each link/contains removal request.
}
\description{
Remove a CONTAINS Relationship
}
